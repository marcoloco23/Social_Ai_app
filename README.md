# Social_Ai_app

1. clone repo
2. cd to repo directory
3. activate venv with python=3.8
4. copy Models(request privately) folder into repo directory
5. pip install -r requirements.txt
6. streamlit run textgenapp.py
